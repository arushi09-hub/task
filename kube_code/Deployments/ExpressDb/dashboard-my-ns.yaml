apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
 name: dashboard-my-ns
 namespace: my-namespace
spec:
 rules:
 - host: dashboard.com
   http:
    paths:
    - backend:
       serviceName: mongodb-service
       servicePort: 27017  